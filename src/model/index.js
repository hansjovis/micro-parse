const InnerNode = require( "./InnerNode" );
const TextNode = require( "./TextNode" );
const CommentNode = require( "./CommentNode" );

/**
 * This module contains the building blocks of an HTML tree.
 *
 * @module model
 */

module.exports = {
	InnerNode,
	TextNode,
	CommentNode,
};
